SOURCES_DIR="notebooklm_app/sources"
OUTPUT_DIR="notebooklm_app/outputs"
NOTEBOOK_TITLE_PREFIX="NotebookLM Batch"
EPISODE_FOCUS=""
LANGUAGE_CODE="en-US"
POLL_INTERVAL=30
POLL_TIMEOUT=900
MAX_CONCURRENCY=4
NLM_AUTHUSER=2

# NotebookLM Enterprise project configuration
GCLOUD_PROJECT_NUMBER="743883644627"
GCLOUD_PROJECT_ID="podcast-generation-477500"
GCLOUD_LOCATION="global"
GCLOUD_ENDPOINT_LOCATION="global"
GCLOUD_DISCOVERY_API_VERSION="v1alpha"
ENTERPRISE_PROJECT_URL="https://notebooklm.cloud.google.com/global/?project=743883644627&authuser=1&pageId=none"

# Optional: point to a specific nlm binary (defaults to looking up 'nlm' on PATH).
# NLM_BIN="$HOME/go/bin/nlm"

# Optional: override MIME type detection when uploading (example: text/markdown)
# UPLOAD_MIME_OVERRIDE="text/markdown"

# Audio export options
#OUTPUT_AUDIO_FORMAT=mp3          # Use "wav" to skip ffmpeg conversion
#MP3_BITRATE=258k                 # Bitrate fed to ffmpeg when OUTPUT_AUDIO_FORMAT=mp3
#KEEP_WAV=0                       # Set to 1 to keep the original WAV alongside the MP3
